import { VideoConference } from '@livekit/components-react';

export const Room = () => {
  // The VideoConference component is an all-in-one component
  // that provides a full video chat UI automatically.
  return <VideoConference />;
};